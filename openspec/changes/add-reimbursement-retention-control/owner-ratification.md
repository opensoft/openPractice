# Owner Ratification Record

Status: **PENDING — SPECKIT FEATURE CREATION IS NOT AUTHORIZED**

This record is the mandatory durable owner gate for OpenSpec change `add-reimbursement-retention-control`. Creating a Speckit feature before this record is completed exactly as required is prohibited.

## Required Validation Evidence

- OpenSpec version: _pending_
- Validation command: `openspec validate add-reimbursement-retention-control --strict`
- Validation timestamp: _pending_
- Exact ratified artifact revision or checksums: _pending_
- Validation exit code: _pending_
- Validation output or durable evidence reference: _pending_

## Required Owner Decision

- Owner identity: _pending_
- Ratification timestamp: _pending_
- Decision: _pending; must be exactly `APPROVED UNCONDITIONALLY`_

Required owner statement:

> I reviewed the exact OpenSpec artifact revision identified above and ratify `add-reimbursement-retention-control` unconditionally. I authorize creation of exactly one Speckit feature for this change. I understand that its plan phase remains blocked until the repository constitution is adopted through separate governance work and a meaningful Constitution Check passes, that runtime work is confined to openPractice unless separately governed elsewhere, and that this OpenSpec change cannot be archived until the implementation and validation are merged and accepted.

## Gate Semantics

- An agent, delegate, inferred preference, conditional approval, blank field, failed strict validation, or ratification of a different artifact revision does not satisfy this gate.
- Editing this template does not itself constitute ratification; the identified owner must provide the completed durable record.
- Until every required field is complete and the decision is exactly `APPROVED UNCONDITIONALLY`, no Speckit feature may be created for this change.
- The proposal's controlled `Status` must remain `draft` until the owner completes this record; only the owner may change it to `ratified`, in the same durable revision as this completed record.
