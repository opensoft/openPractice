## 1. Owner Ratification Gate

- [ ] 1.1 Run `openspec validate add-reimbursement-retention-control --strict` against the exact artifacts proposed for ratification. Acceptance evidence: command, OpenSpec version, timestamp, exact artifact revision or checksums, exit code 0, and validation output are durably recorded in `owner-ratification.md`.
- [ ] 1.2 Obtain the owner's unconditional ratification before any Speckit feature is created. Acceptance evidence: the owner, not an agent or delegate, completes `owner-ratification.md` with identity, timestamp, exact artifact revision, strict-validation evidence, and the unqualified `APPROVED UNCONDITIONALLY` decision, and changes the proposal's controlled `Status` from `draft` to `ratified` in the same durable revision; blank, conditional, inferred, delegated, or status-mismatched approval keeps this gate closed.

## 2. Single Speckit Handoff and Planning Gates

- [ ] 2.1 From the repository's intended base branch, create exactly one Speckit feature referencing `add-reimbursement-retention-control` and the completed owner ratification. Acceptance evidence: one and only one generated `specs/NNN-*` feature/worktree owns all three capability specs, and a repository search finds no second reimbursement-retention feature or duplicate implementation checklist.
- [ ] 2.2 Complete Speckit clarification for the single feature without starting implementation. Acceptance evidence: accepted answers are applied to `spec.md`, material ambiguities about atomicity, conforming intent/use-event fields and decision bindings, separate linked success/failure/indeterminate outcome events, external reconciliation, jurisdiction gating, holds, public-envelope profiles, and destruction claims are resolved or explicitly blocked, and the clarification command result is recorded.
- [ ] 2.3 Before invoking the Speckit plan phase, complete repository constitution adoption as separate governance work, not inside the reimbursement product feature. Acceptance evidence: `.specify/memory/constitution.md` contains no template placeholders, identifies its version and ratification date, and links to the separate owner-approved governance record; the current placeholder constitution does not satisfy this task.
- [ ] 2.4 Before invoking or proceeding with the Speckit plan phase, perform a meaningful Constitution Check against the separately adopted constitution version. Acceptance evidence: the feature records the constitution version, maps each applicable principle and gate to concrete feature obligations, identifies any violation, and blocks planning until every violation is resolved or separately approved under the constitution's own governance process.
- [ ] 2.5 Constrain the feature plan to openPractice runtime changes in the single generated worktree. Acceptance evidence: the plan's file and integration inventory shows no runtime edits, migrations, deployment changes, or implementation tasks in another repository; external systems appear only as bounded contracts, test doubles, or assumptions, and any external-runtime work is deferred to separately governed changes.
- [ ] 2.6 Generate and analyze the single feature's implementation plan and executable tasks, leaving this OpenSpec checklist governance-only. Acceptance evidence: Speckit artifacts trace every delta requirement and scenario to planned work and verification, `speckit.analyze` reports no unresolved critical inconsistency, and no second Speckit feature is created.

## 3. Acceptance and Closure

- [ ] 3.1 Complete implementation and verification only through the single Speckit feature. Acceptance evidence: `closure-evidence.md` references the feature's accepted test and review records covering atomic local rollback; atomic creation of a fully attributable intent/use event with the same authorization decision bindings, pending mutation, and idempotent outbox item; separate immutable linked reconciliation outcome events for success, failure, and indeterminate results; no false success when reconciliation or outcome persistence fails; exact version-pinned public-envelope conformance with private associations kept separate; no stable correlatable public identifiers; active-hold deletion and key-destruction blocking; unselected-jurisdiction duration blocking; successor obligations; and scoped provider destruction claims.
- [ ] 3.2 Verify the no-cross-repository-runtime boundary after implementation. Acceptance evidence: `closure-evidence.md` records final changed-file and dependency inventories showing runtime changes only in openPractice, and any separately governed external work is referenced but not included in this feature.
- [ ] 3.3 Merge the single feature's implementation and validation into the intended base branch before archival. Acceptance evidence: merged commit or pull-request identifier, exact merged head, required check results, review disposition, and feature validation summary are recorded in `closure-evidence.md`.
- [ ] 3.4 Reconcile the merged implementation against all OpenSpec deltas and run final strict validation. Acceptance evidence: `closure-evidence.md` maps every requirement and scenario to accepted implementation or test evidence, records `openspec validate add-reimbursement-retention-control --strict` exiting 0 on the final change revision, and confirms that no acceptance, constitution, legal-hold, jurisdiction, public-envelope, atomicity, or reconciliation gap remains.
- [ ] 3.5 Archive `add-reimbursement-retention-control` only after tasks 3.1 through 3.4 are complete. Acceptance evidence: `closure-evidence.md` identifies the merged implementation, accepted validation evidence, final strict-validation result, delta-spec sync disposition, archive date and path, and confirms that exactly one Speckit feature delivered the change.
